export interface Alert {
    email: string;
    keywords: string;   // e.g. "React, Next.js"
    location?: string;  // optional
    type?: string;      // Full-time, Remote, Internship etc.
  }
  